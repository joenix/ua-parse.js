import system from './system';
import systemVersion from './system_version';

import engine from './engine';
import engineVersion from './engine_version';

import browser from './browser';
import browserVersion from './browser_version';

import shell from './shell';
import shellVersion from './shell_version';

import platform from './platform';

import compare from './compare';

export {
  system,
  systemVersion,
  engine,
  engineVersion,
  browser,
  browserVersion,
  shell,
  shellVersion,
  platform,
  compare
};
